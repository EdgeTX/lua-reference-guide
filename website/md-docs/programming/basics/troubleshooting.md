# Troubleshooting

When a script does not behave as expected, check the basics first:

- file location and file name
- returned table structure
- syntax errors
- missing required assets or modules
- target radio limitations

Start with the simplest failure point before assuming the API itself is wrong.
