<!--
GENERATED FILE — do not hand-edit.
Regenerated by `tools/docs_pipeline.py build` (see docs-system/README.md).
To fix API facts (syntax, params, returns, availability): edit the upstream
EdgeTX /*luadoc*/ comment, then re-extract and rebuild.
To add narrative content (examples, long description, compat notes): add or
edit docs-system/overlays/<doc_id>.md, then rebuild.
-->

# Review: lvgl.page

[Back to dashboard](review.md) | [Open API page](display-lvgl/lvgl-page.md)

## Snapshot

- Module: `lvgl`
- Current symbol: `lvgl.page`
- Doc id: `lvgl.page`
- Source: `radio/src/lua/api_colorlcd_lvgl.cpp:1231`
- Recommended action: **No action needed**
- Why: Parsed output looks structurally healthy.

## Decide

- This one looks structurally complete, but you can still change any param or return type below if you want to override it.

### Decision Controls

<div class="decision-panel" data-item-id="lvgl.page">
<div class="decision-status" data-role="status">Decision server not connected yet.</div>
<table class="decision-table">
<thead><tr><th>Field</th><th>Kind</th><th>Current Type</th><th>Description</th><th>Select Type</th></tr></thead>
<tbody>
<tr><td><code>params</code></td><td>param</td><td><code>table</code></td><td>* `title` (string or function) title text in the header; function support added in 2.11.4, defaults to an empty string
   * `subtitle` (string or function) sub-title text in the header; function support added in 2.11.4, defaults to an empty string
   * `icon` (string) full path to a 30x30 grey-scale mask image for the back button icon (white = transparent, black = opaque); defaults to the EdgeTX logo icon
   * `back` (function) called when RTN is pressed, or when the back button is tapped -- the top-left header button if `backButton` is not set, otherwise a second button on the top-right, defaults to nil
   * `menu` (function) called when the top-left header button is tapped, but only if `backButton` is set to true -- in that case `back` moves to a second button on the top-right, defaults to nil
   * `prevButton` (table) adds a prev navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `nextButton` (table) adds a next navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `flexFlow` (lvgl.FLOW_COLUMN or lvgl.FLOW_ROW) enables flex layout for this page, not used by default
   * `flexPad` (number) padding between rows/columns when flex layout is used, defaults to `PAD_OUTLINE`
   * `scrollBar` (boolean) enables/disables scroll bars, defaults to true
   * `scrollDir` (lvgl.SCROLL_xx) allowed scroll directions when child objects extend past the page bounds (stand alone scripts only), defaults to `lvgl.SCROLL_ALL`
   * `scrolled` (function) called when the page content is scrolled; passed `x`, `y` of the current scroll position, defaults to nil
   * `scrollTo` (function) overrides the page scroll position; must return `x`, `y` to scroll to, defaults to nil
   * `align` (alignment type (LEFT, RIGHT, CENTER, VTOP, VBOTTOM, VCENTER)) alignment used with flex layouts (added in 2.11.4), defaults to `CENTER | VTOP`
   * `backButton` (boolean) if true, adds a second header button on the top-right calling `back`, and repurposes the top-left button to call `menu` instead (added in 2.11.4), defaults to false
   * `borderPad` (number or table) border padding around the container edges; a single number applies to all sides, or a table `{left=?, right=?, top=?, bottom=?}` (added in 2.11.5), defaults to `PAD_OUTLINE` if `flexFlow` is set, otherwise 0</td><td><select data-field-kind="param" data-field-name="params" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="params" data-custom-type="true" /></td></tr>
<tr><td><code>-</code></td><td>return</td><td><code>table</code></td><td>LVGL object, or nil if not running as a One-Time script or fullscreen widget</td><td><select data-field-kind="return" data-field-name="-" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="return" data-field-name="-" data-custom-type="true" /></td></tr>
</tbody>
</table>
<div class="decision-actions"><button type="button" data-role="save">Save Decisions</button></div>
<script type="application/json" class="decision-payload">{&quot;item_id&quot;: &quot;lvgl.page&quot;, &quot;symbol&quot;: &quot;lvgl.page&quot;, &quot;summary&quot;: &quot;Create a page layout for a One-Time script using EdgeTX styling: a header with title/sub-title lines and a back button in the top-left corner. Should be created as the top-level LVGL object, with all other objects added as its children.&quot;, &quot;parameters&quot;: [{&quot;name&quot;: &quot;params&quot;, &quot;type&quot;: &quot;table&quot;, &quot;required&quot;: true, &quot;description&quot;: &quot;* `title` (string or function) title text in the header; function support added in 2.11.4, defaults to an empty string\n   * `subtitle` (string or function) sub-title text in the header; function support added in 2.11.4, defaults to an empty string\n   * `icon` (string) full path to a 30x30 grey-scale mask image for the back button icon (white = transparent, black = opaque); defaults to the EdgeTX logo icon\n   * `back` (function) called when RTN is pressed, or when the back button is tapped -- the top-left header button if `backButton` is not set, otherwise a second button on the top-right, defaults to nil\n   * `menu` (function) called when the top-left header button is tapped, but only if `backButton` is set to true -- in that case `back` moves to a second button on the top-right, defaults to nil\n   * `prevButton` (table) adds a prev navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil\n   * `nextButton` (table) adds a next navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil\n   * `flexFlow` (lvgl.FLOW_COLUMN or lvgl.FLOW_ROW) enables flex layout for this page, not used by default\n   * `flexPad` (number) padding between rows/columns when flex layout is used, defaults to `PAD_OUTLINE`\n   * `scrollBar` (boolean) enables/disables scroll bars, defaults to true\n   * `scrollDir` (lvgl.SCROLL_xx) allowed scroll directions when child objects extend past the page bounds (stand alone scripts only), defaults to `lvgl.SCROLL_ALL`\n   * `scrolled` (function) called when the page content is scrolled; passed `x`, `y` of the current scroll position, defaults to nil\n   * `scrollTo` (function) overrides the page scroll position; must return `x`, `y` to scroll to, defaults to nil\n   * `align` (alignment type (LEFT, RIGHT, CENTER, VTOP, VBOTTOM, VCENTER)) alignment used with flex layouts (added in 2.11.4), defaults to `CENTER | VTOP`\n   * `backButton` (boolean) if true, adds a second header button on the top-right calling `back`, and repurposes the top-left button to call `menu` instead (added in 2.11.4), defaults to false\n   * `borderPad` (number or table) border padding around the container edges; a single number applies to all sides, or a table `{left=?, right=?, top=?, bottom=?}` (added in 2.11.5), defaults to `PAD_OUTLINE` if `flexFlow` is set, otherwise 0&quot;}], &quot;returns&quot;: [{&quot;name&quot;: &quot;&quot;, &quot;type&quot;: &quot;table&quot;, &quot;description&quot;: &quot;LVGL object, or nil if not running as a One-Time script or fullscreen widget&quot;}], &quot;notes&quot;: [&quot;Only available for One-Time scripts and widgets running in full screen mode -- returns nil otherwise.\n  @notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.page` -- only the settings listed above apply.\n  @notice The page automatically adds scroll bars if child objects fall outside its boundaries.\n  @notice `scrollBar`, `scrollDir`, `scrolled`, and `scrollTo` were added in 2.11.2.\n  @notice The PAGE keys are not automatically mapped to `prevButton`/`nextButton` -- the script&#x27;s `run` function must detect and handle them itself.&quot;], &quot;since&quot;: &quot;2.11.0&quot;}</script>
</div>

<div class="review-workbench">
<section class="review-panel">
### Source luadoc

~~~~text
@function lvgl.page(params)

  Create a page layout for a One-Time script using EdgeTX styling: a header with title/sub-title lines and a back button in the top-left corner. Should be created as the top-level LVGL object, with all other objects added as its children.

  @param params (table):
   * `title` (string or function) title text in the header; function support added in 2.11.4, defaults to an empty string
   * `subtitle` (string or function) sub-title text in the header; function support added in 2.11.4, defaults to an empty string
   * `icon` (string) full path to a 30x30 grey-scale mask image for the back button icon (white = transparent, black = opaque); defaults to the EdgeTX logo icon
   * `back` (function) called when RTN is pressed, or when the back button is tapped -- the top-left header button if `backButton` is not set, otherwise a second button on the top-right, defaults to nil
   * `menu` (function) called when the top-left header button is tapped, but only if `backButton` is set to true -- in that case `back` moves to a second button on the top-right, defaults to nil
   * `prevButton` (table) adds a prev navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `nextButton` (table) adds a next navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `flexFlow` (lvgl.FLOW_COLUMN or lvgl.FLOW_ROW) enables flex layout for this page, not used by default
   * `flexPad` (number) padding between rows/columns when flex layout is used, defaults to `PAD_OUTLINE`
   * `scrollBar` (boolean) enables/disables scroll bars, defaults to true
   * `scrollDir` (lvgl.SCROLL_xx) allowed scroll directions when child objects extend past the page bounds (stand alone scripts only), defaults to `lvgl.SCROLL_ALL`
   * `scrolled` (function) called when the page content is scrolled; passed `x`, `y` of the current scroll position, defaults to nil
   * `scrollTo` (function) overrides the page scroll position; must return `x`, `y` to scroll to, defaults to nil
   * `align` (alignment type (LEFT, RIGHT, CENTER, VTOP, VBOTTOM, VCENTER)) alignment used with flex layouts (added in 2.11.4), defaults to `CENTER | VTOP`
   * `backButton` (boolean) if true, adds a second header button on the top-right calling `back`, and repurposes the top-left button to call `menu` instead (added in 2.11.4), defaults to false
   * `borderPad` (number or table) border padding around the container edges; a single number applies to all sides, or a table `{left=?, right=?, top=?, bottom=?}` (added in 2.11.5), defaults to `PAD_OUTLINE` if `flexFlow` is set, otherwise 0

  @retval table LVGL object, or nil if not running as a One-Time script or fullscreen widget

  @notice Only available for One-Time scripts and widgets running in full screen mode -- returns nil otherwise.
  @notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.page` -- only the settings listed above apply.
  @notice The page automatically adds scroll bars if child objects fall outside its boundaries.
  @notice `scrollBar`, `scrollDir`, `scrolled`, and `scrollTo` were added in 2.11.2.
  @notice The PAGE keys are not automatically mapped to `prevButton`/`nextButton` -- the script's `run` function must detect and handle them itself.

  @status current Introduced in 2.11.0, title/subtitle function support, menu, prevButton, nextButton, align, and backButton added in 2.11.4, borderPad added in 2.11.5, scrollBar/scrollDir/scrolled/scrollTo added in 2.11.2
~~~~
</section>
<section class="review-panel">
### Parsed doc

#### Summary

Create a page layout for a One-Time script using EdgeTX styling: a header with title/sub-title lines and a back button in the top-left corner. Should be created as the top-level LVGL object, with all other objects added as its children.

#### Parameters

| Name | Req | Type | Description |
| --- | --- | --- | --- |
| `params` | yes | `table` | * `title` (string or function) title text in the header; function support added in 2.11.4, defaults to an empty string    * `subtitle` (string or function) sub-title text in the header; function support added in 2.11.4, defaults to an empty string    * `icon` (string) full path to a 30x30 grey-scale mask image for the back button icon (white = transparent, black = opaque); defaults to the EdgeTX logo icon    * `back` (function) called when RTN is pressed, or when the back button is tapped -- the top-left header button if `backButton` is not set, otherwise a second button on the top-right, defaults to nil    * `menu` (function) called when the top-left header button is tapped, but only if `backButton` is set to true -- in that case `back` moves to a second button on the top-right, defaults to nil    * `prevButton` (table) adds a prev navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil    * `nextButton` (table) adds a next navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil    * `flexFlow` (lvgl.FLOW_COLUMN or lvgl.FLOW_ROW) enables flex layout for this page, not used by default    * `flexPad` (number) padding between rows/columns when flex layout is used, defaults to `PAD_OUTLINE`    * `scrollBar` (boolean) enables/disables scroll bars, defaults to true    * `scrollDir` (lvgl.SCROLL_xx) allowed scroll directions when child objects extend past the page bounds (stand alone scripts only), defaults to `lvgl.SCROLL_ALL`    * `scrolled` (function) called when the page content is scrolled; passed `x`, `y` of the current scroll position, defaults to nil    * `scrollTo` (function) overrides the page scroll position; must return `x`, `y` to scroll to, defaults to nil    * `align` (alignment type (LEFT, RIGHT, CENTER, VTOP, VBOTTOM, VCENTER)) alignment used with flex layouts (added in 2.11.4), defaults to `CENTER | VTOP`    * `backButton` (boolean) if true, adds a second header button on the top-right calling `back`, and repurposes the top-left button to call `menu` instead (added in 2.11.4), defaults to false    * `borderPad` (number or table) border padding around the container edges; a single number applies to all sides, or a table `{left=?, right=?, top=?, bottom=?}` (added in 2.11.5), defaults to `PAD_OUTLINE` if `flexFlow` is set, otherwise 0 |

#### Returns

| Name | Type | Description |
| --- | --- | --- |
| `-` | `table` | LVGL object, or nil if not running as a One-Time script or fullscreen widget |

#### Notes

- Only available for One-Time scripts and widgets running in full screen mode -- returns nil otherwise.
  @notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.page` -- only the settings listed above apply.
  @notice The page automatically adds scroll bars if child objects fall outside its boundaries.
  @notice `scrollBar`, `scrollDir`, `scrolled`, and `scrollTo` were added in 2.11.2.
  @notice The PAGE keys are not automatically mapped to `prevButton`/`nextButton` -- the script's `run` function must detect and handle them itself.

</section>
</div>

## Suggested luadoc patch

Use this as a starting point when the issue is in the C++ annotation. `TODO` means you still need to choose the real type.

~~~~text
@function lvgl.page([params])

Create a page layout for a One-Time script using EdgeTX styling: a header with title/sub-title lines and a back button in the top-left corner. Should be created as the top-level LVGL object, with all other objects added as its children.

@param params (table) * `title` (string or function) title text in the header; function support added in 2.11.4, defaults to an empty string
   * `subtitle` (string or function) sub-title text in the header; function support added in 2.11.4, defaults to an empty string
   * `icon` (string) full path to a 30x30 grey-scale mask image for the back button icon (white = transparent, black = opaque); defaults to the EdgeTX logo icon
   * `back` (function) called when RTN is pressed, or when the back button is tapped -- the top-left header button if `backButton` is not set, otherwise a second button on the top-right, defaults to nil
   * `menu` (function) called when the top-left header button is tapped, but only if `backButton` is set to true -- in that case `back` moves to a second button on the top-right, defaults to nil
   * `prevButton` (table) adds a prev navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `nextButton` (table) adds a next navigation button; table has a `press` function and optional `active` function (added in 2.11.4), defaults to nil
   * `flexFlow` (lvgl.FLOW_COLUMN or lvgl.FLOW_ROW) enables flex layout for this page, not used by default
   * `flexPad` (number) padding between rows/columns when flex layout is used, defaults to `PAD_OUTLINE`
   * `scrollBar` (boolean) enables/disables scroll bars, defaults to true
   * `scrollDir` (lvgl.SCROLL_xx) allowed scroll directions when child objects extend past the page bounds (stand alone scripts only), defaults to `lvgl.SCROLL_ALL`
   * `scrolled` (function) called when the page content is scrolled; passed `x`, `y` of the current scroll position, defaults to nil
   * `scrollTo` (function) overrides the page scroll position; must return `x`, `y` to scroll to, defaults to nil
   * `align` (alignment type (LEFT, RIGHT, CENTER, VTOP, VBOTTOM, VCENTER)) alignment used with flex layouts (added in 2.11.4), defaults to `CENTER | VTOP`
   * `backButton` (boolean) if true, adds a second header button on the top-right calling `back`, and repurposes the top-left button to call `menu` instead (added in 2.11.4), defaults to false
   * `borderPad` (number or table) border padding around the container edges; a single number applies to all sides, or a table `{left=?, right=?, top=?, bottom=?}` (added in 2.11.5), defaults to `PAD_OUTLINE` if `flexFlow` is set, otherwise 0

@retval (table) LVGL object, or nil if not running as a One-Time script or fullscreen widget

@notice Only available for One-Time scripts and widgets running in full screen mode -- returns nil otherwise.
  @notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.page` -- only the settings listed above apply.
  @notice The page automatically adds scroll bars if child objects fall outside its boundaries.
  @notice `scrollBar`, `scrollDir`, `scrolled`, and `scrollTo` were added in 2.11.2.
  @notice The PAGE keys are not automatically mapped to `prevButton`/`nextButton` -- the script's `run` function must detect and handle them itself.

@status current Introduced in 2.11.0
~~~~
