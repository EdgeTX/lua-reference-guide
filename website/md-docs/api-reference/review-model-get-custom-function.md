<!--
GENERATED FILE — do not hand-edit.
Regenerated by `tools/docs_pipeline.py build` (see docs-system/README.md).
To fix API facts (syntax, params, returns, availability): edit the upstream
EdgeTX /*luadoc*/ comment, then re-extract and rebuild.
To add narrative content (examples, long description, compat notes): add or
edit docs-system/overlays/<doc_id>.md, then rebuild.
-->

# Review: model.getCustomFunction

[Back to dashboard](review.md) | [Open API page](model-functions/model-get-custom-function.md)

## Snapshot

- Module: `model`
- Current symbol: `model.getCustomFunction`
- Doc id: `model.get-custom-function`
- Source: `radio/src/lua/api_model.cpp:1385`
- Recommended action: **No action needed**
- Why: Parsed output looks structurally healthy.

## Decide

- This one looks structurally complete, but you can still change any param or return type below if you want to override it.

### Decision Controls

<div class="decision-panel" data-item-id="model.getCustomFunction">
<div class="decision-status" data-role="status">Decision server not connected yet.</div>
<table class="decision-table">
<thead><tr><th>Field</th><th>Kind</th><th>Current Type</th><th>Description</th><th>Select Type</th></tr></thead>
<tbody>
<tr><td><code>function</code></td><td>param</td><td><code>integer</code></td><td>custom function number (use 0 for CF1)</td><td><select data-field-kind="param" data-field-name="function" data-current-type="integer"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="function" data-custom-type="true" /></td></tr>
<tr><td><code>-</code></td><td>return</td><td><code>nil</code></td><td>requested custom function does not exist</td><td><select data-field-kind="return" data-field-name="-" data-current-type="nil"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="return" data-field-name="-" data-custom-type="true" /></td></tr>
<tr><td><code>-</code></td><td>return</td><td><code>table</code></td><td>custom function data:
* `switch` (number) switch index
 * `func` (number) function index
 * `name` (string)  Name of track to play (only returned only returned if action is play track, sound or script)
 * `value` (number) value (only returned only returned if action is **not** play track, sound or script)
 * `mode` (number) mode (only returned only returned if action is **not** play track, sound or script)
 * `param` (number) parameter (only returned only returned if action is **not** play track, sound or script)
 * `active` (number) 0 = disabled, 1 = enabled
 * `repetition` (number) -1 to 60, range and meaning depend on function</td><td><select data-field-kind="return" data-field-name="-" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="return" data-field-name="-" data-custom-type="true" /></td></tr>
</tbody>
</table>
<div class="decision-actions"><button type="button" data-role="save">Save Decisions</button></div>
<script type="application/json" class="decision-payload">{&quot;item_id&quot;: &quot;model.getCustomFunction&quot;, &quot;symbol&quot;: &quot;model.getCustomFunction&quot;, &quot;summary&quot;: &quot;Get Custom Function parameters&quot;, &quot;parameters&quot;: [{&quot;name&quot;: &quot;function&quot;, &quot;type&quot;: &quot;integer&quot;, &quot;required&quot;: true, &quot;description&quot;: &quot;custom function number (use 0 for CF1)&quot;}], &quot;returns&quot;: [{&quot;name&quot;: &quot;&quot;, &quot;type&quot;: &quot;nil&quot;, &quot;description&quot;: &quot;requested custom function does not exist&quot;}, {&quot;name&quot;: &quot;&quot;, &quot;type&quot;: &quot;table&quot;, &quot;description&quot;: &quot;custom function data:\n* `switch` (number) switch index\n * `func` (number) function index\n * `name` (string)  Name of track to play (only returned only returned if action is play track, sound or script)\n * `value` (number) value (only returned only returned if action is **not** play track, sound or script)\n * `mode` (number) mode (only returned only returned if action is **not** play track, sound or script)\n * `param` (number) parameter (only returned only returned if action is **not** play track, sound or script)\n * `active` (number) 0 = disabled, 1 = enabled\n * `repetition` (number) -1 to 60, range and meaning depend on function&quot;}], &quot;notes&quot;: [], &quot;since&quot;: &quot;2.0.0&quot;}</script>
</div>

<div class="review-workbench">
<section class="review-panel">
### Source luadoc

~~~~text
@function model.getCustomFunction(function)

Get Custom Function parameters

@param function (unsigned number) custom function number (use 0 for CF1)

@retval nil requested custom function does not exist

@retval table custom function data:
 * `switch` (number) switch index
 * `func` (number) function index
 * `name` (string)  Name of track to play (only returned only returned if action is play track, sound or script)
 * `value` (number) value (only returned only returned if action is **not** play track, sound or script)
 * `mode` (number) mode (only returned only returned if action is **not** play track, sound or script)
 * `param` (number) parameter (only returned only returned if action is **not** play track, sound or script)
 * `active` (number) 0 = disabled, 1 = enabled
 * `repetition` (number) -1 to 60, range and meaning depend on function

@status current Introduced in 2.0.0, TODO rename function
~~~~
</section>
<section class="review-panel">
### Parsed doc

#### Summary

Get Custom Function parameters

#### Parameters

| Name | Req | Type | Description |
| --- | --- | --- | --- |
| `function` | yes | `integer` | custom function number (use 0 for CF1) |

#### Returns

| Name | Type | Description |
| --- | --- | --- |
| `-` | `nil` | requested custom function does not exist |
| `-` | `table` | custom function data: * `switch` (number) switch index  * `func` (number) function index  * `name` (string)  Name of track to play (only returned only returned if action is play track, sound or script)  * `value` (number) value (only returned only returned if action is **not** play track, sound or script)  * `mode` (number) mode (only returned only returned if action is **not** play track, sound or script)  * `param` (number) parameter (only returned only returned if action is **not** play track, sound or script)  * `active` (number) 0 = disabled, 1 = enabled  * `repetition` (number) -1 to 60, range and meaning depend on function |

#### Notes

None.

</section>
</div>

## Suggested luadoc patch

Use this as a starting point when the issue is in the C++ annotation. `TODO` means you still need to choose the real type.

~~~~text
@function model.getCustomFunction([function])

Get Custom Function parameters

@param function (integer) custom function number (use 0 for CF1)

@retval (nil) requested custom function does not exist

@retval (table) custom function data:
* `switch` (number) switch index
 * `func` (number) function index
 * `name` (string)  Name of track to play (only returned only returned if action is play track, sound or script)
 * `value` (number) value (only returned only returned if action is **not** play track, sound or script)
 * `mode` (number) mode (only returned only returned if action is **not** play track, sound or script)
 * `param` (number) parameter (only returned only returned if action is **not** play track, sound or script)
 * `active` (number) 0 = disabled, 1 = enabled
 * `repetition` (number) -1 to 60, range and meaning depend on function

@status current Introduced in 2.0.0
~~~~
