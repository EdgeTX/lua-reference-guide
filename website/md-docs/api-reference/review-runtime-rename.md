<!--
GENERATED FILE — do not hand-edit.
Regenerated by `tools/docs_pipeline.py build` (see docs-system/README.md).
To fix API facts (syntax, params, returns, availability): edit the upstream
EdgeTX /*luadoc*/ comment, then re-extract and rebuild.
To add narrative content (examples, long description, compat notes): add or
edit docs-system/overlays/<doc_id>.md, then rebuild.
-->

# Review: rename

[Back to dashboard](review.md) | [Open API page](filesystem/rename.md)

## Snapshot

- Module: `runtime`
- Current symbol: `rename`
- Doc id: `runtime.rename`
- Source: `radio/src/lua/api_filesystem.cpp:226`
- Recommended action: **No action needed**
- Why: Parsed output looks structurally healthy.

## Decide

- This one looks structurally complete, but you can still change any param or return type below if you want to override it.

### Decision Controls

<div class="decision-panel" data-item-id="rename">
<div class="decision-status" data-role="status">Decision server not connected yet.</div>
<table class="decision-table">
<thead><tr><th>Field</th><th>Kind</th><th>Current Type</th><th>Description</th><th>Select Type</th></tr></thead>
<tbody>
<tr><td><code>from_path</code></td><td>param</td><td><code>string</code></td><td>Current path to rename @param to_path (string) Path to rename to</td><td><select data-field-kind="param" data-field-name="from_path" data-current-type="string"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="from_path" data-custom-type="true" /></td></tr>
</tbody>
</table>
<div class="decision-actions"><button type="button" data-role="save">Save Decisions</button></div>
<script type="application/json" class="decision-payload">{&quot;item_id&quot;: &quot;rename&quot;, &quot;symbol&quot;: &quot;rename&quot;, &quot;summary&quot;: &quot;Rename a file or directory\n  If the file or directory is being moved to a new parent directory, then the new parent\n  directory must already exist.&quot;, &quot;parameters&quot;: [{&quot;name&quot;: &quot;from_path&quot;, &quot;type&quot;: &quot;string&quot;, &quot;required&quot;: true, &quot;description&quot;: &quot;Current path to rename @param to_path (string) Path to rename to&quot;}], &quot;returns&quot;: [], &quot;notes&quot;: [], &quot;since&quot;: &quot;2.11.0&quot;}</script>
</div>

<div class="review-workbench">
<section class="review-panel">
### Source luadoc

~~~~text
@function rename(from_path, to_path)

 Rename a file or directory
  If the file or directory is being moved to a new parent directory, then the new parent
  directory must already exist.

@param from_path (string) Current path to rename
@param to_path (string) Path to rename to

@status current Introduced in 2.11.0

 Returns FRESULT (e.g. 0=OK, 6=Path invalid, 8=Directory exists)
~~~~
</section>
<section class="review-panel">
### Parsed doc

#### Summary

Rename a file or directory
  If the file or directory is being moved to a new parent directory, then the new parent
  directory must already exist.

#### Parameters

| Name | Req | Type | Description |
| --- | --- | --- | --- |
| `from_path` | yes | `string` | Current path to rename @param to_path (string) Path to rename to |

#### Returns

None.

#### Notes

None.

</section>
</div>

## Suggested luadoc patch

Use this as a starting point when the issue is in the C++ annotation. `TODO` means you still need to choose the real type.

~~~~text
@function rename([from_path])

Rename a file or directory
  If the file or directory is being moved to a new parent directory, then the new parent
  directory must already exist.

@param from_path (string) Current path to rename @param to_path (string) Path to rename to

@status current Introduced in 2.11.0
~~~~
