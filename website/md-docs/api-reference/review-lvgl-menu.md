<!--
GENERATED FILE — do not hand-edit.
Regenerated by `tools/docs_pipeline.py build` (see docs-system/README.md).
To fix API facts (syntax, params, returns, availability): edit the upstream
EdgeTX /*luadoc*/ comment, then re-extract and rebuild.
To add narrative content (examples, long description, compat notes): add or
edit docs-system/overlays/<doc_id>.md, then rebuild.
-->

# Review: lvgl.menu

[Back to dashboard](review.md) | [Open API page](display-lvgl/lvgl-menu.md)

## Snapshot

- Module: `lvgl`
- Current symbol: `lvgl.menu`
- Doc id: `lvgl.menu`
- Source: `radio/src/lua/api_colorlcd_lvgl.cpp:1320`
- Recommended action: **Fix C++ annotation first**
- Why: unknown return types: none

## Decide

- Unknown returns to decide: `none`
- Use the selector table below. Your choices are saved into `docs-system/generated/review-decisions.json`, which I can read later to patch the C++ `luadoc` block.

### Decision Controls

<div class="decision-panel" data-item-id="lvgl.menu">
<div class="decision-status" data-role="status">Decision server not connected yet.</div>
<table class="decision-table">
<thead><tr><th>Field</th><th>Kind</th><th>Current Type</th><th>Description</th><th>Select Type</th></tr></thead>
<tbody>
<tr><td><code>params</code></td><td>param</td><td><code>table</code></td><td>* `title` (string) text displayed in the popup&#x27;s header, defaults to an empty string
   * `values` (table) simple table of option strings shown in the popup, defaults to an empty list
   * `get` (function) called when the popup is opened, to get the index of the currently selected option (1..number of values), defaults to nil
   * `set` (function) called when the user taps a menu item; passed the selected item&#x27;s index (1..number of values), defaults to nil</td><td><select data-field-kind="param" data-field-name="params" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="params" data-custom-type="true" /></td></tr>
<tr><td><code>none</code></td><td>return</td><td><code>unknown</code></td><td></td><td><select data-field-kind="return" data-field-name="none" data-current-type="TODO"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="return" data-field-name="none" data-custom-type="true" /></td></tr>
</tbody>
</table>
<div class="decision-actions"><button type="button" data-role="save">Save Decisions</button></div>
<script type="application/json" class="decision-payload">{&quot;item_id&quot;: &quot;lvgl.menu&quot;, &quot;symbol&quot;: &quot;lvgl.menu&quot;, &quot;summary&quot;: &quot;Open a popup menu with multiple options to choose from, using EdgeTX styling.&quot;, &quot;parameters&quot;: [{&quot;name&quot;: &quot;params&quot;, &quot;type&quot;: &quot;table&quot;, &quot;required&quot;: true, &quot;description&quot;: &quot;* `title` (string) text displayed in the popup&#x27;s header, defaults to an empty string\n   * `values` (table) simple table of option strings shown in the popup, defaults to an empty list\n   * `get` (function) called when the popup is opened, to get the index of the currently selected option (1..number of values), defaults to nil\n   * `set` (function) called when the user taps a menu item; passed the selected item&#x27;s index (1..number of values), defaults to nil&quot;}], &quot;returns&quot;: [{&quot;name&quot;: &quot;none&quot;, &quot;type&quot;: &quot;unknown&quot;, &quot;description&quot;: &quot;&quot;}], &quot;notes&quot;: [&quot;The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.menu` -- only the settings listed above apply.\n  @notice The popup closes and `set` is called when the user selects an item.\n  @notice If the user taps outside the popup or presses RTN, the popup closes without calling `set`.&quot;], &quot;since&quot;: &quot;2.11.1&quot;}</script>
</div>

<div class="review-workbench">
<section class="review-panel">
### Source luadoc

~~~~text
@function lvgl.menu(params)

  Open a popup menu with multiple options to choose from, using EdgeTX styling.

  @param params (table):
   * `title` (string) text displayed in the popup's header, defaults to an empty string
   * `values` (table) simple table of option strings shown in the popup, defaults to an empty list
   * `get` (function) called when the popup is opened, to get the index of the currently selected option (1..number of values), defaults to nil
   * `set` (function) called when the user taps a menu item; passed the selected item's index (1..number of values), defaults to nil

  @retval none

  @notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.menu` -- only the settings listed above apply.
  @notice The popup closes and `set` is called when the user selects an item.
  @notice If the user taps outside the popup or presses RTN, the popup closes without calling `set`.

  @status current Introduced in 2.11.1
~~~~
</section>
<section class="review-panel">
### Parsed doc

#### Summary

Open a popup menu with multiple options to choose from, using EdgeTX styling.

#### Parameters

| Name | Req | Type | Description |
| --- | --- | --- | --- |
| `params` | yes | `table` | * `title` (string) text displayed in the popup's header, defaults to an empty string    * `values` (table) simple table of option strings shown in the popup, defaults to an empty list    * `get` (function) called when the popup is opened, to get the index of the currently selected option (1..number of values), defaults to nil    * `set` (function) called when the user taps a menu item; passed the selected item's index (1..number of values), defaults to nil |

#### Returns

| Name | Type | Description |
| --- | --- | --- |
| `none` | `unknown` |  |

#### Notes

- The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.menu` -- only the settings listed above apply.
  @notice The popup closes and `set` is called when the user selects an item.
  @notice If the user taps outside the popup or presses RTN, the popup closes without calling `set`.

</section>
</div>

## Suggested luadoc patch

Use this as a starting point when the issue is in the C++ annotation. `TODO` means you still need to choose the real type.

~~~~text
@function lvgl.menu([params])

Open a popup menu with multiple options to choose from, using EdgeTX styling.

@param params (table) * `title` (string) text displayed in the popup's header, defaults to an empty string
   * `values` (table) simple table of option strings shown in the popup, defaults to an empty list
   * `get` (function) called when the popup is opened, to get the index of the currently selected option (1..number of values), defaults to nil
   * `set` (function) called when the user taps a menu item; passed the selected item's index (1..number of values), defaults to nil

@retval none (TODO)

@notice The common object settings (x, y, w, h, color, etc.) are not used by `lvgl.menu` -- only the settings listed above apply.
  @notice The popup closes and `set` is called when the user selects an item.
  @notice If the user taps outside the popup or presses RTN, the popup closes without calling `set`.

@status current Introduced in 2.11.1
~~~~
