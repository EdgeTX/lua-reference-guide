<!--
GENERATED FILE — do not hand-edit.
Regenerated by `tools/docs_pipeline.py build` (see docs-system/README.md).
To fix API facts (syntax, params, returns, availability): edit the upstream
EdgeTX /*luadoc*/ comment, then re-extract and rebuild.
To add narrative content (examples, long description, compat notes): add or
edit docs-system/overlays/<doc_id>.md, then rebuild.
-->

# Review: lvgl.build

[Back to dashboard](review.md) | [Open API page](display-lvgl/lvgl-build.md)

## Snapshot

- Module: `lvgl`
- Current symbol: `lvgl.build`
- Doc id: `lvgl.build`
- Source: `radio/src/lua/api_colorlcd_lvgl.cpp:453`
- Recommended action: **No action needed**
- Why: Parsed output looks structurally healthy.

## Decide

- This one looks structurally complete, but you can still change any param or return type below if you want to override it.

### Decision Controls

<div class="decision-panel" data-item-id="lvgl.build">
<div class="decision-status" data-role="status">Decision server not connected yet.</div>
<table class="decision-table">
<thead><tr><th>Field</th><th>Kind</th><th>Current Type</th><th>Description</th><th>Select Type</th></tr></thead>
<tbody>
<tr><td><code>parent</code></td><td>param</td><td><code>object</code></td><td>optional LVGL object to attach the built objects to; if omitted they attach to the top-level script window</td><td><select data-field-kind="param" data-field-name="parent" data-current-type="object"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="parent" data-custom-type="true" /></td></tr>
<tr><td><code>params</code></td><td>param</td><td><code>table</code></td><td>a table of tables, each defining one LVGL object:
* `type` (string or type constant) mandatory, selects the kind of object to create, e.g. `&quot;rectangle&quot;` or `lvgl.RECTANGLE`; named type constants (e.g. `lvgl.RECTANGLE`) added in 2.11.4, string names work in all versions
 * `name` (string) optional name used to look up this object in the returned table, defaults to an empty string
 * `children` (table) optional nested table of the same shape, to create child objects
 * any other key accepted by the constructor function for the chosen `type`</td><td><select data-field-kind="param" data-field-name="params" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="param" data-field-name="params" data-custom-type="true" /></td></tr>
<tr><td><code>-</code></td><td>return</td><td><code>table</code></td><td>named LVGL objects (only entries whose definition included a `name` are included)</td><td><select data-field-kind="return" data-field-name="-" data-current-type="table"><option value="TODO">TODO</option><option value="integer">integer</option><option value="number">number</option><option value="string">string</option><option value="boolean">boolean</option><option value="table">table</option><option value="function">function</option><option value="pointer">pointer</option><option value="nil">nil</option><option value="integer|string">integer|string</option><option value="table|nil">table|nil</option><option value="function|nil">function|nil</option><option value="string|nil">string|nil</option></select><input type="text" placeholder="Custom type" data-field-kind="return" data-field-name="-" data-custom-type="true" /></td></tr>
</tbody>
</table>
<div class="decision-actions"><button type="button" data-role="save">Save Decisions</button></div>
<script type="application/json" class="decision-payload">{&quot;item_id&quot;: &quot;lvgl.build&quot;, &quot;symbol&quot;: &quot;lvgl.build&quot;, &quot;summary&quot;: &quot;Build a complex UI in a single operation from a table of object definitions.&quot;, &quot;parameters&quot;: [{&quot;name&quot;: &quot;parent&quot;, &quot;type&quot;: &quot;object&quot;, &quot;required&quot;: false, &quot;description&quot;: &quot;optional LVGL object to attach the built objects to; if omitted they attach to the top-level script window&quot;}, {&quot;name&quot;: &quot;params&quot;, &quot;type&quot;: &quot;table&quot;, &quot;required&quot;: true, &quot;description&quot;: &quot;a table of tables, each defining one LVGL object:\n* `type` (string or type constant) mandatory, selects the kind of object to create, e.g. `\&quot;rectangle\&quot;` or `lvgl.RECTANGLE`; named type constants (e.g. `lvgl.RECTANGLE`) added in 2.11.4, string names work in all versions\n * `name` (string) optional name used to look up this object in the returned table, defaults to an empty string\n * `children` (table) optional nested table of the same shape, to create child objects\n * any other key accepted by the constructor function for the chosen `type`&quot;}], &quot;returns&quot;: [{&quot;name&quot;: &quot;&quot;, &quot;type&quot;: &quot;table&quot;, &quot;description&quot;: &quot;named LVGL objects (only entries whose definition included a `name` are included)&quot;}], &quot;notes&quot;: [&quot;Objects can be nested via `children`, another table of tables using the same shape as the top-level `params`.\n@notice Very large or deeply nested tables may fail to compile to `.luac` -- if a script works from `.lua` but fails from `.luac`, split the call into multiple smaller `lvgl.build()` calls.&quot;], &quot;since&quot;: &quot;2.11.0&quot;}</script>
</div>

<div class="review-workbench">
<section class="review-panel">
### Source luadoc

~~~~text
@function lvgl.build([parent, ]params)

Build a complex UI in a single operation from a table of object definitions.

@param parent (object) optional LVGL object to attach the built objects to; if omitted they attach to the top-level script window

@param params (table) a table of tables, each defining one LVGL object:
 * `type` (string or type constant) mandatory, selects the kind of object to create, e.g. `"rectangle"` or `lvgl.RECTANGLE`; named type constants (e.g. `lvgl.RECTANGLE`) added in 2.11.4, string names work in all versions
 * `name` (string) optional name used to look up this object in the returned table, defaults to an empty string
 * `children` (table) optional nested table of the same shape, to create child objects
 * any other key accepted by the constructor function for the chosen `type`

@retval table named LVGL objects (only entries whose definition included a `name` are included)

@notice Objects can be nested via `children`, another table of tables using the same shape as the top-level `params`.
@notice Very large or deeply nested tables may fail to compile to `.luac` -- if a script works from `.lua` but fails from `.luac`, split the call into multiple smaller `lvgl.build()` calls.

@status current Introduced in 2.11.0, named type constants added in 2.11.4
~~~~
</section>
<section class="review-panel">
### Parsed doc

#### Summary

Build a complex UI in a single operation from a table of object definitions.

#### Parameters

| Name | Req | Type | Description |
| --- | --- | --- | --- |
| `parent` | no | `object` | optional LVGL object to attach the built objects to; if omitted they attach to the top-level script window |
| `params` | yes | `table` | a table of tables, each defining one LVGL object: * `type` (string or type constant) mandatory, selects the kind of object to create, e.g. `"rectangle"` or `lvgl.RECTANGLE`; named type constants (e.g. `lvgl.RECTANGLE`) added in 2.11.4, string names work in all versions  * `name` (string) optional name used to look up this object in the returned table, defaults to an empty string  * `children` (table) optional nested table of the same shape, to create child objects  * any other key accepted by the constructor function for the chosen `type` |

#### Returns

| Name | Type | Description |
| --- | --- | --- |
| `-` | `table` | named LVGL objects (only entries whose definition included a `name` are included) |

#### Notes

- Objects can be nested via `children`, another table of tables using the same shape as the top-level `params`.
@notice Very large or deeply nested tables may fail to compile to `.luac` -- if a script works from `.lua` but fails from `.luac`, split the call into multiple smaller `lvgl.build()` calls.

</section>
</div>

## Suggested luadoc patch

Use this as a starting point when the issue is in the C++ annotation. `TODO` means you still need to choose the real type.

~~~~text
@function lvgl.build([parent, params])

Build a complex UI in a single operation from a table of object definitions.

@param parent (object) optional LVGL object to attach the built objects to; if omitted they attach to the top-level script window

@param params (table) a table of tables, each defining one LVGL object:
* `type` (string or type constant) mandatory, selects the kind of object to create, e.g. `"rectangle"` or `lvgl.RECTANGLE`; named type constants (e.g. `lvgl.RECTANGLE`) added in 2.11.4, string names work in all versions
 * `name` (string) optional name used to look up this object in the returned table, defaults to an empty string
 * `children` (table) optional nested table of the same shape, to create child objects
 * any other key accepted by the constructor function for the chosen `type`

@retval (table) named LVGL objects (only entries whose definition included a `name` are included)

@notice Objects can be nested via `children`, another table of tables using the same shape as the top-level `params`.
@notice Very large or deeply nested tables may fail to compile to `.luac` -- if a script works from `.lua` but fails from `.luac`, split the call into multiple smaller `lvgl.build()` calls.

@status current Introduced in 2.11.0
~~~~
